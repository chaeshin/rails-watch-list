<div class="card-product">
  <img src=<%= movie.poster_url %>>
  <div class="card-product-infos">
    <h2><strong><%= movie.title %></strong></h2>
    <p><%= movie.rating %></p>
    <p><%= link_to 'delete', bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: 'Delete this movie?' }  %></p>
    <p><%= movie.overview %></p>
  </div>
</div>
